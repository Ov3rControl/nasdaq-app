{
    "buildCommand": "pnpm --filter web build && mkdir -p dist && if [ -d apps/web/dist ]; then cp -r apps/web/dist/* ./dist/; fi && cp ./dist/index.html ./dist/404.html",
    "outputDirectory": "dist",
    "installCommand": "pnpm install",
    "framework": null,
    "routes": [
      {
        "src": "/assets/(.*)",
        "headers": { "cache-control": "s-maxage=31536000,immutable" },
        "continue": true
      },
      {
        "src": "/(.*)",
        "dest": "/index.html"
      }
    ]
  }
  